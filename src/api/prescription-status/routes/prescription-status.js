'use strict';

/**
 * prescription-status router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::prescription-status.prescription-status');
